# Pothole Detection — Python dependencies
# Tested on Python 3.10. See README for CUDA guidance.

# ---- core training / inference ----
ultralytics>=8.2.0          # YOLOv8m + RT-DETR-L, training + prediction
torch>=2.1.0                # CPU/CUDA: install matching wheel from pytorch.org
torchvision>=0.16.0         # patched at runtime by rocm_compat.py for ROCm
numpy>=1.26.0
opencv-python>=4.9.0
Pillow>=10.0.0
PyYAML>=6.0

# ---- DETR fine-tuning notebook ----
transformers>=4.40.0        # facebook/detr-resnet-50
accelerate>=0.30.0
timm>=0.9.16
datasets>=2.18.0
pycocotools>=2.0.7

# ---- experiment tracking ----
wandb>=0.16.0

# ---- evaluation / plotting / curation utilities ----
pandas>=2.1.0
matplotlib>=3.8.0
scikit-learn>=1.3.0
tqdm>=4.66.0

# ---- Streamlit demo ----
streamlit>=1.32.0

# ---- dataset download (optional) ----
kaggle>=1.6.0
